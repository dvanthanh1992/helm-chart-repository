apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "thanh-demo-app-py-chart.fullname" . }}-test-connection"
  labels:
    {{- include "thanh-demo-app-py-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "thanh-demo-app-py-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
