apiVersion: v1
kind: Service
metadata:
  name: {{ include "thanh-demo-app-py-chart.fullname" . }}
  labels:
    {{- include "thanh-demo-app-py-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "thanh-demo-app-py-chart.selectorLabels" . | nindent 4 }}
