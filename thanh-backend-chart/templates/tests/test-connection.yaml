apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tony-be.fullname" . }}-test-connection"
  labels:
    {{- include "tony-be.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tony-be.fullname" . }}-stable:{{ .Values.service.port }}']
  restartPolicy: Never
